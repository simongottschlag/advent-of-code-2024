import Foundation
import Testing

@testable import days

@Suite("day02ParseLine")
struct day02ParseLineTests {
    @Test func parseLineSucceeds() {
        // let input: String.SubSequence = "7 6 4 2 1"
        // let result = day02.parseLine(input)
        // guard case .success(let line) = result else {
        //     Issue.record("Should never be reached")
        //     return
        // }

        // #expect(line == [7, 6, 4, 2, 1])
    }
}
